1) install swagger

a) go install github.com/swaggo/swag/cmd/swag@latest

b) Install required packages:

go get github.com/swaggo/http-swagger
go get github.com/swaggo/files


c) Import them in your Go code:

import (
    httpSwagger "github.com/swaggo/http-swagger"
    _ "your-module-path/docs" // important: import the docs package
)

d) swag init

e) http://localhost:9093/swagger/index.html
